### 3.1 States

| State | Description | Initial State? | Terminal State? |
| --- | --- | --- | --- |
{ #each row in states }
| { row.name } | { row.description } | { row.initial } | { row.terminal } |
{ /each }
