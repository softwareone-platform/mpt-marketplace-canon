## 4. Business Rules

| Rule ID | Rule Statement | Applies In State(s) | Actor Scope | Implements | Notes |
| --- | --- | --- | --- | --- | --- |
{ #each row in rules }
| { row.id } | { row.statement } | { row.states } | { row.actor_scope } | { row.implements } | { row.notes } |
{ /each }
