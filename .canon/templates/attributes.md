## 5. Key Attributes

| Attribute | Type | Description | Set By | Mutable After Creation? | Notes |
| --- | --- | --- | --- | --- | --- |
{ #each row in attributes }
| { row.name } | { row.type } | { row.description } | { row.set_by } | { row.mutable } | { row.notes } |
{ /each }
