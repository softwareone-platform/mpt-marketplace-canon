### 7.2 Cross-Object State Effects

| Triggering Event | Affected Object | Effect on Affected Object | Automated? | Condition | Notes |
| --- | --- | --- | --- | --- | --- |
{ #each row in effects }
| { row.trigger } | { row.affected } | { row.effect } | { row.automated } | { row.condition } | { row.notes } |
{ /each }
